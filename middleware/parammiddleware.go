package middleware

func InitParamValidation() {
	//return func(c *gin.Context) {
	//if v, ok := binding.Validator.Engine().(*validator.Validate); ok {
	//	// 这里的 key 和 fn 可以不一样最终在 struct 使用的是 key
	//	err1 := v.RegisterValidation("OnlyCapitalAndLower", tools.onlyCapitalAndLower)
	//	if err1 != nil {
	//		panic(err1)
	//	}
	//	err2 := v.RegisterValidation("MustCapitalLowerNum", tools.mustCapitalLowerNum)
	//	if err2 != nil {
	//		panic(err2)
	//	}
}
